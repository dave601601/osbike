# 플랜트 / passive 안정성

## 2026-07-07 — 클린 플랜트 재측정: 주행 시 회복한계 ≥10°, self-steering 물리 정상 확인

### 조향법칙 정착 (refactor)
`controller.py`: yaw-hold `heading()` 폐기 → `steer()` = `k_ls·(lean + 0.25·roll̇) −
kp_steer·steer − kd_steer·steeṙ`. 0.25s = 불안정 시정수(1/√(Mgh/I_p)) → "예측 lean"
해석. `lqr_gains.json` 스키마 갱신(k_ls/kp_steer/kd_steer), viz/render_bike 단일 소스화.
실코드 경로(rollout.rollout) 검증: 정지 균형 / 정지출발→2m/s(v_end 1.99) / 발사 주행
모두 5000/5000. `best_gains.json`(구 스키마)·`render_balance.py`(중복) 삭제.

### 재측정 결과 (클린 플랜트)
**① 회복 한계각** — 새 발견: **주행이 정지보다 회복력이 훨씬 큼**
| 섭동 | 정지 | 주행 2m/s |
|---|---|---|
| ~7° | OK | OK |
| 8~10° | 낙하(~1s) | **OK (10°까지 전부)** |

정지 한계 7~8°는 플라이휠 정적 한계 asin(τ_max/Mgh)=8.4°와 정합. 주행 시 카운터스티어가
플라이휠 한계를 넘는 회복 권한을 제공 — "속도가 균형을 돕는다"의 정량 확인.

**② passive self-steering (무제어, v0=2)**: lean +0.04~0.13(오른쪽) 동안 steer가
−0.1°→−8.3°(오른쪽)로 전개 = **기운 쪽으로 자동 조향 (교과서 물리)**. 생존 2.8s
(구 0.6~0.8s). 주의: lean+는 오른쪽 / steer+는 왼쪽이라 "같은 쪽" = 곱 음수 —
브레이크 제거 전 [B] 배터리의 "부호일치 YES(곱 양수)"는 사실 반대쪽으로 돌던 것
(브레이크가 포크를 끌고 다님). 클린 플랜트에서 비로소 올바른 방향.
passive 자가안정 속도창은 2m/s에선 없음(포크 오버슛) — 고속 스윕은 추후.

**③ 타이어폭 basin (tire_test 재실행)**: 수치 미세 변화(예: 4cm@5° 1.74→1.12s),
결론 동일 — basin ≈ atan(반폭/h) 정확히 유지. 브레이크는 내부력이라 전복 거동엔
영향 작았음(추정대로).

## 2026-07-07 — 유령 브레이크 발견·제거: downtube가 앞바퀴를 1.9kN으로 짓누르고 있었음

### 결론
**"전진 안 됨" 문제의 진범은 스크럽/구름저항이 아니라 모델 버그.** downtube 캡슐 끝단
(1.0, 0, 0.05)이 앞바퀴 원판 내부에 33mm 관통 → 상시 접촉 법선력 **1,886 N** → 앞바퀴에
브레이크 패드가 눌린 상태. 무제어 관성주행이 2 m/s에서 2초 만에 정지할 정도.
frame↔front_wheel은 조부모-손자 관계라 MuJoCo filterparent(부모-자식만 제외)가 안 걸림.
`<contact><exclude body1="frame" body2="front_wheel"/></contact>` 로 해결. **초기 커밋부터
존재** — 이전의 모든 구동/저항 관련 정량 수치는 오염됨(방향성 결론은 대체로 유지).

### 판별 과정
1. ellipsoid(둥근 crown) 타이어로 바꿔도 동일 급감속 → 타이어 모서리 가설 기각.
2. 감속률 2~4 m/s² = 30~50N급 항력 → 마찰 스크럽 스케일 초과 → "브레이크" 의심.
3. 접촉 목록 덤프 → downtube↔front_geom 침투 -33mm, 법선력 1886N 확인.

### 수정 후 재검증 (전부 20s 완주, 2° 섭동, 플라이휠LQR + 조향(센터링±lean PD) + 속도PI)
| 시나리오 | 결과 |
|---|---|
| 관성주행 v0=2 | 2.00→1.12 m/s 완만 감속 (0.044 m/s², 정상 구름손실) |
| 구동 발사 v0=2 | **2.00 m/s 정착 유지** |
| 구동 정지출발 | 4s에 2 m/s 도달(오버슛 2.34), 이후 2.0 추종 |
| 정지 균형 v_t=0 | 20s 유지 |
| 주행균형 k_ls∈{-5,-2,0,+2} | 전 셀 STABLE, 조향 요동 미미 (k_ls=0도 됨: trail+센터링+플라이휠로 충분) |

### 오염된 과거 수치 (재측정 필요, 방향성은 유지)
- 8° 회복 한계(토크포화), passive 타이어폭 basin 수치, self-steering 응답 수치,
  trail 배터리 [A~D] 수치. — 내부 힘이라 gross 거동 영향은 제한적이었을 것으로 추정되나
  정량 인용 전 재실행 필수.
- "weaving 스크럽" 가설 폐기. 속도추종 문제 자체가 소멸.

### 교훈
새 body 추가 시(특히 moving-mass 슬라이더) `d.ncon` 덤프로 의도치 않은 내부 접촉부터 확인할 것.

## 2026-07-07 — 조향축 12° caster 추가 (trail 74mm): self-steering 생성, 주행균형 달성

### 결론
v1의 수직 조향축(trail=0)이 포크잼·주행균형 실패·self-steering 부재의 공통 원인이었다.
조향축을 12° 후경(`axis="-0.2079 0 0.9781"`) → **trail 74.4mm 실측**(실차 투어링급).

### 검증 (2° 섭동, 20s 호라이즌)
1. **Self-steering 생성 [B]**: 완전 무제어에서 포크가 lean 쪽으로 스스로 꺾임
   (0.2s에 +14~46°, v0=0~4 전부 부호 일치). 제안서 핵심 메커니즘이 시뮬에 존재하게 됨.
   단 조향 감쇠가 작아(0.02) 스톱(45°)까지 오버슛 → passive 단독으론 여전히 낙하
   (자가안정 속도창 없음). 조향 frictionloss/damper 추가 검토 여지.
2. **주행균형 달성 [C]**: lean→steer PD(+센터링+감쇠) `u_st=k_ls·lean+(k_ls/4)·roll̇−2·steer−0.5·steeṙ`
   + 플라이휠 LQR + 속도PI 조합, **여러 셀이 5000/5000 (20s) STABLE**, max|steer|≤17°(잼 없음):
   발사 v0=2: k_ls=−2, +2 STABLE / 정지출발: k_ls=−5, −2 STABLE.
   trail 이전 최고는 11.4s+포크 46° 포화였음.
3. **포크잼 해소 [D]**: 구 드라이브 설정에서 steer가 더는 45°에 안 박힘(max 34°),
   v_fwd 0.18→0.55. 단 stale yaw-hold gain 때문에 9.1s에 낙하.
4. **회귀 [A]**: 구 정지균형 설정(LQR+yaw-hold 10/2)은 새 플랜트에서 **6.1s로 퇴행**
   (wheel-flop과 yaw-hold가 상충). → yaw-hold 손 gain은 폐기 대상.
   같은 정지 조건에서 [C]의 새 조향법칙(k_ls=−2)은 20s STABLE — 대체 확인.

### 미해결
- **속도 추종 여전히 실패**: STABLE 셀들도 v_fwd≈0.25m/s로 크롤링(목표 2). 발사해도 감속.
  균형과 분리된 구동/저항 문제 (스크럽·구름저항 의심).
- `lqr_gains.json`+`viz.py`의 yaw-hold 설정은 새 플랜트에서 넘어짐 — 조향법칙을
  steer각 피드백 구조로 바꿔야 (controller.heading() 개편 or MIMO LQR).

## 2026-07-06 — 무제어 낙하 + 타이어 폭의 영향 (baseline)

### 결론
무제어(ctrl=0)에서 현재 타이어(반폭 0.8cm / 전폭 1.6cm)는 **0.5° 섭동에도 1.7초 안에 넘어짐**
= 실질적 역진자. 타이어 폭을 넓히면 정적 안정 basin이 **정확히 `atan(반폭/h_CoM)`** 대로 커짐.
→ "얇은 타이어라 진짜 균형 문제"라는 설계 전제 정량 검증. LQR이 실제 일을 하고 있음이 확인됨.

### 무제어 낙하 시간 [초] (5000스텝=20s, up_z<0.7 기준)
| 반폭(전폭) | 0.5° | 1° | 2° | 5° | 10° | 정적한계 atan(w/h) |
|---|---|---|---|---|---|---|
| 0.8cm(1.6cm) ← 현재 | 1.72 | 1.39 | 1.12 | 0.80 | 0.57 | 0.9° |
| 2.0cm(4cm) | STABLE | STABLE | STABLE | 0.89 | 0.60 | 2.2° |
| 4.0cm(8cm) | STABLE | STABLE | STABLE | 1.74 | 0.70 | 4.4° |
| 8.0cm(16cm) | STABLE | STABLE | STABLE | STABLE | 0.89 | 8.7° |
| 15cm(30cm) | STABLE | STABLE | STABLE | STABLE | STABLE | 16.1° |

STABLE/낙하 경계가 예측 basin `atan(w/h)`와 거의 정확히 일치 (h=0.52m).
예: 4cm반폭 한계 4.4° → 2°는 유지, 5°는 낙하. 8cm 한계 8.7° → 5° 유지, 10° 낙하.

### 메커니즘
실린더 타이어를 forward축(x) 기준으로 기울이면 접지가 타이어 가장자리(±반폭)로 이동,
CoM 투영이 ±반폭 안이면 정적 복원. lean > atan(w/h) 넘으면 tipping. 얇을수록 basin≈0.

### 참고
- 완벽 직립(0°)에서는 대칭이라 무제어로도 안 넘어짐(불안정 평형에 정확히 얹힘) — 섭동 필요.
- 질량은 1.2kg 고정, 폭만 변수(관성만 미세 변화) → 통제된 비교.

### 파일
- `tire_test.py` — 폭×섭동 그리드, XML fromto 문자열 주입 (디스크 XML 불변)
- `render_bike.py` — `NOCONTROL=1`, `TIRE_HALFWIDTH=` env 지원 (격자바닥)
- `nocontrol_fall_16mm.mp4` — 현재 타이어 무제어 낙하 (2°→1.1s, 커밋 제외)
