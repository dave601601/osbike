# moving-mass + free-fork (연구 타겟 플랜트)

## 2026-07-07 — ★ 보강②: heading 폭주 진단 → lateral/heading 외곽루프. 코스유지 30m→0.1m

### 진단: 경사 "드리프트"의 실체는 사이드슬립이 아니라 heading 상실
40-seed 맵의 생존 run들이 |y_end| 중앙값 30m를 표류 → 시계열 추적: slip각 ~1°뿐인데
**yaw가 명령 +30°를 무시하고 내리막으로 -60°(1°경사)/-66°(2°경사) 폭주**. 원인 사슬:
1) lean_max 0.5°는 경사에서 heading 권한 부족 — 경사각만큼의 lean은 유효중력에 수직이라
   슬라이더 부담 ≈0인데도 캡에 막힘 (2°경사 유지엔 지속 lean ~2.5° 필요, 3°캡이면 잡힘).
2) P-only heading(k_yaw=0.2)은 상주오차가 구조적 (3° lean을 내려면 15° 오차 상주).
→ **생존-only 채점은 "내리막 항복" cheat 가능** (LQR도 RL도 마찬가지) — 코스 지표 병기.

### 처방 (`mm_controller.py` — 전부 슬루/스케줄 가드 필수였음)
- **lean_max 0.5°→3°** + **heading 적분 ki_yaw=0.05** (포화 중 적분 정지 anti-windup).
- **lateral 외곽루프 k_lat=0.08**: 기준경로(명령 방위로 v_target 적분한 앵커) crosstrack
  → yaw_ref 보정. **슬루 1.5°/s + ±12° 클립 필수** — 즉발 보정은 선회 램프(회전하는
  기준선)에서 e/ė 요동으로 전 seed 낙하 (lean≥1°+k_lat 즉발 조합 전멸 관측).
- **속도 스케줄 frac** = clip((v_t−0.75)/(1.5−0.75), 0, 1) 을 lean캡·ki_yaw·k_lat에 곱 —
  v≤0.75에선 구 P-only(0.5°캡)로 **정확히 환원**. 저속은 lean당 yaw 권한(g·tanθ/v)이
  커지고 균형도 marginal이라 외곽루프 동역학만으로 낙하 (v0.75: 캡 3°도 1.5°도 낙하,
  0.5°는 생존 — isolation). 스케줄 기준은 실측 v_fwd가 아닌 **v_target** — v가 처질 때
  캡이 같이 처지면 heading 권한 상실→드리프트 악순환 (ct −7→−16m 관측).
- `CtrlState(v_i, yaw_i, lat_corr)` 도입 (구 float integ 하위호환 승격).

### 결과 (40 seeds, `envelope_lqr_klat.json` = 새 정본 baseline)
| sev | 0ms | 4ms | 8ms | 12ms | 16ms | 20ms |
|---|---|---|---|---|---|---|
| s0.25 | 100 | 100 | 100 | 100 | 75 | 70 |
| s0.5 | 98 | 100 | 98 | **55** | 10 | 5 |
| s0.75 | 20 | 18 | 0 | 0 | 0 | 0 |
| s1.0 | 0 | 0 | 0 | 0 | 0 | 0 |

- **코스 유지: |crosstrack| 중앙값 30.5m → 0.1m.** 생존율은 사실상 동일(384→379/960)
  — 표류(cheat)가 생존에 도움이 안 됐음도 확인.
- 셀별: s0.5 8ms 88→98%, 12ms 48→55% 개선 / 16ms 28→10% 악화(경사와 싸우는 대가,
  marginal 대역 재배치). s0.75+는 여전히 붕괴(25→20%) = 지형 자체가 한계.
- 어블레이션 `envelope_lqr_yawpi.json`(k_lat=0): 생존 377, ct중앙 0.4m — heading PI가
  코스의 대부분을 담당, k_lat은 잔여 트림+정밀(0.1m).

### v_min 뉘앙스 (회귀 아님 — 원래 있던 것의 발견)
"v_min≈0.65 ∞"는 **heading off(pure balance) 기준** (docs 수치 0.6→27s/0.65→∞ 정확
재현). heading on(k_yaw=0.2)이면 v_min≈0.70 (0.65는 27s 낙하 — 구/신 컨트롤러가 같은
시각에 낙하 = ② 무회귀 증명). "저속에서 조향까지"는 한 단계 더 어렵다는 정량 포인트.

### RL 명분에 미치는 영향
- baseline이 이제 **코스를 지키면서** 위 생존율 → RL도 동일 코스 유지를 요구해야 공정
  (하네스가 ct_end/ct_max/yaw_end 기록).
- 결합×지연 절벽(s0.5: 98@8ms→55@12ms→5@20ms)과 s0.75 지형 붕괴는 외곽루프 보강으로도
  그대로 → **RL 니치 2개 모두 유지** ("쉬운 고전 보강"으로는 안 닫힘을 확인).

## 2026-07-07 — ★ 결합×지연 맵 40-seed 재측정 (`mm_envelope.py` 공용 하네스) — 구 3-seed 맵 정정

### 왜 (RL 착수 전 보강 ①)
구 맵은 셀당 3 seeds(33% 단위)로 비단조(0·5✓/10✗/15✓/20✗)였고, 실험 코드가 커밋돼 있지
않아 RL과 동일 프로토콜 비교가 불가능했다. → 스윕을 `mm_envelope.py`로 고정(코드+
`envelope_lqr.json` 커밋), seed 40으로 재측정. **이 하네스가 이후 RL 정책의 채점기**
(같은 지형 seed·같은 판정 기준으로 맵을 다시 그려 겹친다).

### 프로토콜 확정 (mm_envelope.py 도큐스트링이 정본)
- v=1.5, 60N LQR(+heading 캐스케이드+속도PI), +30° 선회(t=2s, slew 3°/s), 30s 생존
  (up_z>0.7), 물리 250Hz / 제어 50Hz ZOH.
- **지연 격자 = 물리스텝(4ms) 정수배** [0,4,8,12,16,20]ms. 구 5/10/15ms 라벨은 250Hz에서
  구현 불가능(숨은 양자화) — 정수배만 허용(assert).
- **μ는 바닥+양 바퀴 모두 설정** — MuJoCo 접촉 마찰은 두 geom의 element-wise max라
  바닥만 낮추면 바퀴 1.4가 이겨 마찰 축이 무효가 된다.
- hfield y±60m(경사 드리프트 가짜낙하 방지, 교훈#4), seed→범프 실현. edge(지형이탈) 별도 판정.

### 결과 (생존 %, 40 seeds, edge 0건)
| sev | 0ms | 4ms | 8ms | 12ms | 16ms | 20ms |
|---|---|---|---|---|---|---|
| s0.25 (1°/μ1.1/1cm) | 100 | 100 | 100 | 98 | 85 | 70 |
| s0.5 (2°/μ0.9/2cm) | 100 | 98 | 88 | **48** | 28 | **0** |
| s0.75 (3°/μ0.6/4cm) | **25** | 22 | 0 | 0 | 0 | 0 |
| s1.0 (4°/μ0.4/5cm) | 0 | 0 | 0 | 0 | 0 | 0 |

### 구 맵 대비 정정 (차이는 전부 3-seed 노이즈)
1. **비단조성 소멸** — 40 seeds에선 지연·severity 양축 모두 단조. marginal/chaotic 해석은
   유지되나 "15ms ✓ / 10ms ✗" 같은 개별 셀 주장은 폐기.
2. **s0.75는 지연 0에서도 25%** (구 66%는 3-seed 중 2개 착시). μ 민감도 체크: μ0.6↔1.4
   동일(0/8) — 킬러는 마찰이 아니라 경사3°+범프4cm 조합. → **"결합 robustness 잘 쌓임"은
   ~s0.5까지만** (union 테스트의 3°/μ0.7/3cm ✓✓는 그보다 완한 조합의 소표본이었음).
3. s0.25는 지연에 관대(20ms에도 70%) — 구맵(15ms 33%, 20ms 0%)보다 훨씬 강함.

### 정밀화된 RL 명분 (인용용)
- **주 타겟 = s0.5 × 지연**: 88%@8ms → 48%@12ms → 0%@20ms (50% 교차 ~12ms).
  "결합×지연 캡 ~10-15ms" 주장은 s0.5에서 유지 — 단 절벽이 아닌 완만한 붕괴.
- **추가 gap = s0.75 순수 지형** (지연 0에서 75% 실패, 지연 무관). RL이 개선하면 보너스,
  못 하면 물리 한계 후보 — 어느 쪽이든 결과.
- 실차 지연대(12-20ms) 기준: s0.25 생존(85-98%), s0.5 반토막→전멸, s0.75+ 전멸.

### 함정 (재발 방지)
- **JAX + fork = 교착**: 부모에서 JAX 초기화 후 Pool(fork)하면 워커 전원이 첫 jnp 호출에서
  멈춤 (12워커 12분 무한대기 사고, load 0.01이 단서). multiprocessing은 **spawn** 필수.

## 2026-07-07 — 고전 delay-aware 상한: 평지는 밀지만 결합×지연은 ~10-15ms 캡

### 시도한 고전 기법
1. **gain detune** (앞): 단독지연 20→40ms, 결합에선 효과 미미.
2. **Smith predictor / 상태예측** (감축 4-state 모델로 지연만큼 앞선 상태 예측 → 그걸로 LQR).
   구현: in-flight 명령들을 A_d,B_d로 롤포워드해 x_pred, u=-K·x_pred. (= 지연-증강 LQR 최적해)

### 결과
| 케이스 | 무보상 | Smith predictor |
|---|---|---|
| 평지 단독지연 (250Hz) | 40ms까지 | **60ms까지 (2배↑)** |
| 평지 단독지연 (50Hz) | 20ms까지 | 40ms까지 |
| **결합 s0.5(경사2μ0.9범프2), 10ms** | 100% | **66% (악화!)** |
| 결합 s0.5, 20ms | 33% | 0% (악화) |
| 결합 s0.75, 15ms | 0% | 33% (미미한 개선) |

### 핵심 통찰 (RL 명분의 정밀화)
- **Smith predictor는 평지에선 지연 tolerance 2배로 밀지만, 지형이 끼면 오히려 악화.**
- 이유: 예측기는 **정확한 모델**이 필요 — 감축 4-state가 self-steering·지형을 무시 →
  지형 하에서 예측이 틀림 → 보상이 역효과. (지연-증강 LQR도 같은 모델의존이라 동일 한계.)
- **결론: 내가 아는 고전 delay 기법(detune+Smith predictor=model-based 최적)은 결합×지연에서
  ~10-15ms 캡.** model-based가 지형에 깨지는 게 근본 이유.
- **이것이 model-free RL(지형+지연 결합 랜덤화 학습)의 정당한 니치.** RL이 이 캡을 넘는지가
  제안서의 정량 질문 — 강한 고전 baseline(LQR+detune+Smith) 대비 비교. (RL이 못 넘으면 그것도
  결과: "이 플랜트는 결합×지연에 근본 한계".)

## 2026-07-07 — ★★ 합집합 stress-test: 결합 robustness는 잘 쌓임. 진짜 killer는 "결합×지연"

### ⚠️ 첫 union 결론("robustness 안 쌓임")은 아티팩트였다 (사용자 지적으로 발견)
초기 union이 mild 조합(경사1.5°+범프1cm)에서도 낙하 → 추적하니 **밸런스는 완벽(lean±0.5°),
대신 옆으로 드리프트**(경사 때문, 횡위치 제어 k_lat=0). hfield 지형패치가 y방향 8m뿐이라
**7.6m 드리프트 후 지형 밖으로 떨어진 것**. 밸런스 실패 아님. → 지형 y폭 45m로 넓혀 정정.

### 정정 결과 (넓은 지형, 60N, v=1.5)
| 조합 | 지연0 | 지연20ms |
|---|---|---|
| 경사1.5+μ1.0+범프1.5 | ✓ | ✗ |
| **경사3+μ0.7+범프3 (직진+선회)** | **✓✓** | ✗ |
| 경사4+μ0.5+범프5 | ✗ | ✗ |
| 결합(경사2+μ0.7+범프3) 지연스윕 | 0·5ms ✓ / 10ms ✗ / 15ms ✓ / 20ms ✗ (비단조) |

### 핵심 통찰 (정정판)
- **결합 robustness는 잘 쌓인다**: 지연 없으면 LQR이 **경사3°+저마찰+범프3cm를 동시에**
  (직진·선회 모두) 버팀. "안 쌓인다"는 틀렸음.
- **진짜 killer = 결합 × 지연.** 단독 지연 tolerance ~40ms가 **결합 조건에선 ~5ms로 붕괴**,
  그 위는 marginal/chaotic(비단조). 실차 지연(10~20ms)이면 결합 조건은 마진 소멸.
- **RL 명분(정정)**: "결합에 못 버텨서"가 아니라 **"결합 조건 하에서 지연에 강건한가"**.
  단독 지연은 고전(detune 40ms/Smith)으로 되지만, 결합×지연은 마진이 없어 delay-aware나
  RL+DR(지연 포함 결합 랜덤화)이 필요. joint (지형×지연×기동) robustness가 승부처.
- 부수 발견: 경사에서 **횡위치 드리프트** → 실차엔 lateral/heading 외곽루프(k_lat) 필요
  (경사면 유지 주행). 지금 k_lat=0이라 down-slope로 흘러감.

## 2026-07-07 — LQR stress-test ②: 지형/경사/마찰은 robust (한계는 물리 authority)

### 결과 (60N LQR, v=1.5)
| 외란 | 한계 | 원인(=RL이 못 뚫는 물리) |
|---|---|---|
| 옆경사(cross-slope) | ~5° (7°서 낙하) | 무게추가 옆중력 상쇄 → 17cm서 스트로크 소진 |
| 오르막(long slope) | ~4° | ±4Nm 드라이브가 속도 유지 못해 v_min↓ 낙하(균형 아님) |
| 마찰 μ | **0.1(빙판)까지 OK** | 직진은 옆힘 거의 안 씀 |
| 범프 지형(hfield) | **≥5cm smooth OK** | (더 높은/날카로운 건 미검증) |

### 지형 만드는 법 (참고)
- 경사: `m.opt.gravity` 틸트 (평지+기운 중력 = 경사 등가). 옆경사=y성분, 오르막=x성분.
- 범프: **hfield** — `<asset><hfield nrow ncol size="sx sy zt zb"/></asset>` + `type="hfield"`
  geom, `m.hfield_data[:]` 에 높이(0~1, zt로 스케일). 여기선 gaussian smooth 랜덤.
- 마찰: `m.geom_friction[gid,0]`.

### 종합: RL niche는 어디인가 (stress-test 결론)
- ✅ **고전 LQR로 다 됨**: 균형·저속(≥v_min)·선회·정지출발·마찰(빙판)·옆경사5°·범프5cm.
  한계는 전부 **물리 authority**(스트로크·드라이브·self-steering) → **RL도 못 뚫음**.
- ⚠️ **유일한 진짜 gap = 액추에이터 지연/sim-to-real** (stress-test ①). gain-tune으로 40ms
  캡 → delay-aware(Smith predictor) 또는 RL(DR) 필요. **이게 RL의 순수 premium 후보.**
- → 제안서 결론 방향: "nominal·외란엔 고전 충분, RL의 값은 sim-to-real(특히 지연) 강건성에
  국한" = 강한 baseline 위에서의 정직한 비교. (대부분 RL논문은 약baseline과 비교 → 차별점.)

## 2026-07-07 — LQR stress-test ①: 액추에이터 지연 tolerance ~40ms (셔더링, sim2real 리스크)

### 결과 (60N LQR, 지연버퍼 = 명령 N스텝 늦게 적용)
| 조건 | 낙하 임계 | 저지연 거동 |
|---|---|---|
| 순수 지연(250Hz 제어) | ~60ms | 12ms~ 진동, 20~56ms 2° 셔더 |
| **현실적(50Hz 제어+지연)** | **~40ms** | 8ms~ 셔더, 30ms 2°, 40ms 임계 |
| 저속 v=0.75(50Hz+지연) | 40ms+ | 저속이 약간 관대 |

- **~15ms부터 2° limit-cycle 셔더링** 발생(안 넘어지지만 실차라면 떨림). ~40ms에서 낙하.
- 원인: 약한 액추에이터 → 큰 LQR gain(K0~-47633) → 고gain+지연 = limit cycle. (모든 이슈의
  공통 뿌리.) 50Hz ZOH가 유효지연 추가해 임계를 60→40ms로 낮춤.
- **판정**: 실차 총지연(IMU필터 10~30ms + 50Hz 20ms + 액추에이터)이 40ms 임계에 근접 →
  **genuine sim-to-real 리스크**.

### RL niche 인가? — detune 검증 결과: 고전 gain-tuning은 40ms에서 캡 (delay-aware 필요)
게인 레벨(설계 Fmax)별 최대 무낙하 지연 (60N, 50Hz, v=2):
| 설계Fmax | K0 | 0ms | 최대지연 |
|---|---|---|---|
| 3~5 | -5.5k~-7.3k | **1.76° 셔더**(너무약) | 20ms |
| 10 | -11.5k | 깨끗 | 20ms |
| **20** | **-19.4k** | 깨끗 | **40ms ★스윗스팟** |
| 40~60 | -34k~-48k | 깨끗 | 20ms |

- **detune 비단조**: Fd=20이 최선(40ms). 너무 약하면 빠른 극 못 잡아 셔더+악화, 너무 세면
  지연 민감. **단순 gain-tuning으로 20→40ms가 한계, 그 이상 안 밀림.**
- (참고: 렌더에 쓴 Fd=60은 delay 관점 열등. 권장 운전점 = Fd≈20, K0≈-19k.)
- **결론**: 지연은 gain-tuning만으론 부족 → **delay-aware 고전(Smith predictor/상태예측 LQR)
  또는 RL(DR)** 필요. 40ms 실차 지연에 basic LQR은 marginal. 공정 비교 = **RL vs
  (LQR+Smith predictor)**. 둘 다 지연을 명시 처리 → 다음: 상태예측 LQR로 40ms↑ 가능한지.

## 2026-07-07 — 정지출발(standing start): v=0에서 가속→유지 성공 (초기 기울기 ≤1°)

v=0 정적 균형은 불가하지만, **정지에서 출발해 불안정구간(v<0.65)을 가속으로 통과하면**
살아남는다 (사람이 페달 밟고 출발하듯). 낙하 vs 가속의 경쟁:
| 목표v | 0.5° | 1.0° | 2.0° |
|---|---|---|---|
| 1 m/s | ✅ | ✅ | ❌1.6s |
| 2 m/s | ✅(0.65 도달 0.78s) | ✅ | ❌1.4s |
| 3 m/s | ✅ | ✅ | ❌ |
- 드라이브 ±4Nm로 v=0→0.65를 **0.78s**에 통과(낙하는 ~2s) → 가속이 이김. 이후 lean→0 damp.
- **초기 기울기 ≤1° 성공, 2° 실패**(저속 구간에서 lean이 커져 회복 불가). 가속 빠를수록 robust.
- 렌더 mm_standstart.mp4 (MM_V0=0 옵션). 함의: v=0 "정적 불가"지만 "동적 출발"은 됨 —
  실차 킥스타트 타당. 2°+ 출발엔 더 큰 가속(드라이브 토크) 또는 초기 보조 필요.

## 2026-07-07 — ★ 최소 균형속도 v_min ≈ 0.65 m/s (힘·스트로크 무관, self-steering 한계)

### 핵심 결과 (제안서 control authority 곡선의 균형 축)
2kg·±0.15m·60N, 0.5° 섭동, 30s 기준:
| v [m/s] | 0 | 0.25 | 0.5 | 0.6 | **0.65** | 0.75~2.0 |
|---|---|---|---|---|---|---|
| 결과 | 2s | 4s | 9.6s | 27s | **∞ 5/5** | ∞ 5/5 |

**v ≥ 0.65 m/s 부터 human-like(5/5) 균형, 30s+.** 날카로운 임계(0.6→27s, 0.65→∞).

### v_min은 힘·스트로크로 못 내림 (근본 = self-steering)
- **힘 60→120→200N: v_min 동일**(전부 0.65). 
- **v=0.5에서 스트로크 ±0.15→±1.0m: 전부 9.7s**(동일).
→ 저속 한계는 액추에이터 authority가 아니라 **free-fork self-steering이 속도와 함께
약해지는 것**이 원인. 그 아래선 무게추가 fall을 catch해도 recenter할 self-steering 피드백이
없어 유지 불가(스트로크 소진). v=0가 근본 불가였던 것의 연속.

### 함의
- **moving-mass+free-fork 개념은 ~0.65 m/s까지 저속 균형 가능** (사람 hands-free 최저속 수준).
- v_min을 더 낮추는 레버는 **액추에이터가 아니라 포크 지오메트리(trail↑)** 또는 능동조향·
  리액션휠. → 다음 스윕: trail vs v_min. (제안서의 "설계영역" 질문)
- 고전제어(LQR)로 여기까지 다 풀림 — RL은 v_min 근방/그 이하를 뚫는지가 관전포인트.

## 2026-07-07 — 선회 슬립/속도 분석 (60N): 미끄러짐 없음, 선회속도는 slew·lean 한계

### 슬립 (사용자 우려: 미끄러지나?)
60N·40° 선회(slew3): **거의 완전 구름** — 바퀴 종방향 슬립 0.2cm/s(≈0), 차체 슬립각
평균 0.03°·최대 3.2°, 횡속 평균 0.09cm/s. **옆으로 안 미끄러지고 향한 방향으로 간다.**
텍스쳐 탓에 시각 판단만 어려웠던 것 → top-down 렌더로 궤적 곡선 명확화.

### 선회 속도 (사용자 우려: 너무 느림)
- 느린 원인: lean_max 0.5° → yaw rate g·tan(lean)/v = 2.45°/s 로 묶임 → 40° 23s.
- **lean_max↑로 빨라짐** (60N이라 여유 있음): lean_max≥1°면 slew(명령램프)가 병목.
- **급하게 돌리면 미끄러지며 낙하**: lean_max5°+slew8~25°/s로 90° 시도 → 슬립각 13~18°,
  전복. 선회 aggressiveness에 한계 존재.
- **안전한 빠른 선회**: lean_max3°+slew≤7°/s면 40°를 6~7s에 슬립각 3.3°로 clean.
  (렌더 mm_turn40_fast_top.mp4: slew6·lean3 → 7s, up_z 0.9995)
- 최대 무슬립 선회율은 (힘,속도,타이어마찰) 함수 — authority 곡선의 조향 축.

### 렌더 옵션 추가
`mm_render.py`: `MM_FORCE`(힘+LQR재설계), `MM_LEANMAX`(선회 lean 한계), `MM_CAM=top`.

## 2026-07-07 — 재검토(≥50N): 선회 chaos는 힘부족 아티팩트(해소). v=0는 근본적 불가

힘=60N 재설계(K0=-47633) 후 그동안 "불가"였던 것들 재검토:

### ✅ 선회: chaos 완전 해소 (힘부족이 원흉이었음)
40° 선회(v=2, slew3): 초기섭동 0.48/0.50/0.5001/0.52° **전부 ∞ 생존, 정확히 40.0° 정착,
5/5 human-like, 결정론적**. 20° 선회도 5/5. → **"선회 chaotic" 결론은 20N force-starvation
아티팩트였다.** ≥50N에선 선회가 부드럽고 정확하게 됨.

### ❌ v=0 정지 균형: 힘·스트로크 무관하게 불가 (근본 한계)
| force | stroke | 생존 |
|---|---|---|
| 60 | 0.15~0.50 | 2.5s |
| 60 | 1.0m | 4.4s |
| 120 | 1.0m(!) | 25.6s (그래도 미달) |
매 케이스 **max|y|=스트로크 한계** → travel 소진이 원인. v=0엔 self-steering이 없어 무게추
혼자 역진자를 잡아야 하는데, **유한 스트로크 반작용질량은 리액션휠과 달리 travel이 유한**
→ 언젠가 포화 → 근본적으로 track-stand 불가. (near-standstill: v=0→2.5s, 0.5→7.3s, 2→∞
로 속도 따라 매끄럽게 개선 = self-steering 기여가 속도의존.)

### 정리 (moving-mass 스토리 확정)
1. **20N 스펙이 병목**이었음: 힘 ≥50N이면 직진·선회 모두 부드럽고(5/5) 결정론적.
2. **v=0(완전 정지)은 moving-mass+free-fork로 불가** (스트로크 유한 → 근본). 제안서 표현
   "저속·**준**정지"가 맞음 — 최소 균형속도가 존재. 완전정지엔 리액션휠 등 무한-travel
   액추에이터나 스티어 구동 필요.
3. **최소 균형속도**는 핵심 연구량 — 힘/무게추 사양별로 매핑 대상. (60N K로 v=0.5 아직
   marginal; 앞선 "v≥0.5 성공"은 다른(연질) K·2° 기준이라 컨트롤러 의존 → 별도 매핑 필요.)

## 2026-07-07 — Authority 스윕: 근본 원인은 **힘 부족(20N)**. ≥50N이면 5/5 human-like

### 핵심 (하드웨어 사이징 결론)
질량·스트로크가 아니라 **슬라이더 구동력(force)이 병목**. 2kg·±0.15m 고정, v=2 직진:
| force | freq | 힘포화 | leanRMS | 사람다움 |
|---|---|---|---|---|
| 20N(현재) | 1.9Hz | 94% | 1.9° | **1/5** (bang-bang) |
| 30N | 2.0Hz | 95% | 2.0° | 1/5 |
| 40N | 0.8Hz | 21% | 1.1° | 3/5 |
| **≥50N** | 0.1Hz | 0% | 0.1° | **5/5 ★** |

**~50N 임계**에서 급전환: 그 이하는 포화→limit-cycle(bang-bang), 이상은 충분한 headroom→
선형 제어→초기섭동 damp 후 정지(힘≈0). **현재 20N 스펙이 그동안의 모든 병리
(2Hz slam, 비현실적 motion)의 근본 원인.** 힘만 ≥50N이면 무게추 2kg으로 human-like 균형.

### 2D 스윕에서 확인된 것 (질량·스트로크·힘)
- **질량↑은 힘 없이는 역효과**: 4·8kg + 20~40N은 2s에 낙하 (무거운 추를 약한 힘으로 못
  움직임). 8kg도 80N이면 5/5 — 즉 힘이 질량에 맞게 커야.
- **스트로크↑은 소폭 도움**: 2kg에서 0.15m·40N=3/5 → 0.30m·40N=5/5 (한계접촉 감소).
- 최소 사양 후보: **2kg · ±0.15m · ≥50N** (또는 40N + ±0.30m).

### 캐비앳 + 재검토 필요
- 이 realism 점수는 **외란 없는 단일섭동 정착** 기준 (정착 후 힘≈0). 지속 외란 하 힘수요는
  별도(모터 연속토크 사이징). 초기 catch peak force도 미측정(정상상태 창만).
- **중대 함의**: 앞선 "선회 chaotic / v=0 불가" 결론은 전부 **20N(force-starved)**에서 나온 것.
  ≥50N에서 선회·정지균형이 되살아나는지 **재검토 대상** (over-claim 방지 위해 미검증).

## 2026-07-07 — realism 지표 모듈(`mm_metrics.py`): 사람다움 정량화, 모든 컨트롤러 공통 채점

### 목적
무게추 motion의 '사람다움'을 수치화해 LQR/MPC/RL을 같은 잣대로 평가. `evaluate(gains,...)`
→ dict, `report()` → 리포트카드, `human_score()` → 통과/5.
지표: balance(생존/lean RMS·max) + realism(진폭, peak, 한계접촉%, 주파수, jerk[m/s³],
힘포화%). 사람 기준: 주파수<1Hz, 한계접촉<10%, 힘포화<20%, jerk<10, lean RMS<5°.

### 첫 채점 결과 (사람다움 X/5) — 어떤 고전 컨트롤러도 human-like 아님
| 컨트롤러 | 생존 | 주파수 | 한계접촉 | 힘포화 | jerk | lean RMS | 점수 |
|---|---|---|---|---|---|---|---|
| LQR @ v=2 | ∞ | 1.9Hz✗ | 31%✗ | 84%✗ | 3292✗ | 1.9°✓ | **1/5** |
| LQR @ v=6 | ∞ | 2.1Hz✗ | 30%✗ | 99%✗ | 3820✗ | 3.2°✓ | 1/5 |
| 소게인PD @ v=2 | 1.6s 낙하 | — | — | — | — | 14°✗ | 낙하 |
| 소게인PD @ v=6 | ∞ | 0.2Hz✓ | 70%✗ | 0%✓ | 482✗ | 10°✗ | 2/5 |

### 통찰 (두 실패 모드)
- **LQR = 빠른 slam**: 주파수·jerk·힘포화 다 초과, lean만 낮음.
- **소게인PD = 느린 sloppy wobble**: 힘·주파수는 사람급인데 lean 30°·한계접촉 70%.
- **둘 다 5/5 불가.** "부드럽게 + 안 흔들리게 + 스트로크 여유"를 동시에 만족하는
  컨트롤러가 목표 → MPC(rate·stroke 제약)/RL(smoothness+lean 보상)의 정량 타겟.
- 무게추 자체가 marginal(2kg·±0.15m)이라 5/5가 물리적으로 가능한지도 열린 질문
  (authority 스윕과 연결).

### 관찰 (사용자 지적: "사람은 이렇게 안 흔든다")
현재 LQR 직진 균형(v=2)의 무게추 정상상태:
- 진폭 std **11.4cm** (범위 −16.8~+16.9cm, ±15 한계 넘나듦), **한계에서 33% 시간**
- **주파수 ≈ 1.9 Hz** (초당 2번 풀스윙), 힘 **84~95% 포화(bang-bang)**
→ 사람 hands-free(수 cm, <1Hz, 부드러움)와 전혀 다름. **비현실적.**

### 원인 + 되는/안되는 경계
1. **v=2에서 부드러운 제어 불가.** LQR을 부드럽게(힘·속도 페널티↑) 재설계하면 K0가
   −19351→−3876으로 줄지만 **전부 3~4s에 낙하**. 저속·약한 무게추로 불안정극(τ=0.25s)을
   잡으려면 게인이 커야 하고 → 포화 → bang-bang. **저속 moving-mass-only 균형은 본질적으로
   무게추 혹사가 불가피.**
2. **LQR은 self-steering을 무시** — 해석모델 상태에 steer 없음. 그래서 v=6에서도(플랜트가
   거의 자가안정) LQR은 여전히 무게추를 2Hz·98% 포화로 혹사.
3. **자가안정 속도창 존재**: 무게추 OFF·free fork만으로 **v≥8 m/s 자가안정**(무제어 생존∞).
   v=5~6은 11~13s(거의).
4. **중속(v≥5)에서 작은 게인 PD는 부드러움**: kp=30급이면 **주파수 0.2~0.3Hz, 힘 포화 0%**
   (사람다움). 단 진폭은 아직 ~10cm(스트로크 여유 부족은 남음). = self-steering이 대부분
   맡고 무게추는 살살 거드는 realistic 영역.

### 결론 (사람 비교)
사람은 hands-free를 **중속(self-steering 작동 영역)**에서 **부드러운 CoM**으로 탐 = 위 4번.
우리 제안서 타겟인 **저속(v=2)은 그보다 어려운 영역**(자가안정 아래 + 약한 액추에이터)이라
무게추 혹사가 불가피. **"저속에서 부드럽게 균형"이 진짜 난제이자 RL/MPC 명분** — LQR의
2Hz 포화를, smoothness 보상을 준 RL이나 rate-제약 MPC가 개선할 수 있는지가 핵심 질문.
(단 v=2 물리 한계상 완전 부드럽긴 어려울 것.)

## 2026-07-07 (최종 확정) — 무게추 조향: LQR이 bang-bang으로 포화 → 선회는 chaotic

### 근본 원인 (앞선 두 설명 모두 틀렸음, 이게 진짜)
무게추는 authority가 작아(B 작음) 불안정극(τ=0.25s)을 잡으려면 LQR gain이 거대해짐
(K0 ~ 수천~수만). → **힘이 시간의 85~96% ±20N 포화 = 사실상 relay/bang-bang**.
- **직진·소보정(≈5°): robust 안정.** 대칭이라 bang-bang limit-cycle이 안정 —
  초기섭동 0.49~0.51° 전부 50s 생존.
- **진짜 선회(≥10°): chaotic.** 선회가 슬라이더를 한쪽으로 치우쳐 stroke 여유를 없애면
  bang-bang이 발산 임계로 감. **초기섭동 0.5000°→25s, 0.5001°→9s** (0.0001° 차이로
  낙하시각 급변), 0.48→4s/0.49→22s/0.51→7s — 완전 비예측. "40°에 정착하나?"에
  robust한 yes/no 자체가 없음.

### 왜 그동안 flip-flop 했나 (교훈)
chaotic이라 실행마다 결과가 달라짐: 20s 호라이즌→"성공", 40s→"낙하", CPU vs GPU 상이,
inline vs mm_controller 상이, F_max 설계 바꾸면 또 상이. 각 실행을 결정론으로 오해해
매번 다른 메커니즘("오버슛"/"stroke 소진"/"authority")을 갖다 붙였음. **실제론 하나:
weakly-actuated plant → LQR 거대게인 → bang-bang → 선회 영역에서 chaotic.**
(사용자가 "잘하다 다른 이유로 넘어진다" 관찰로 chaotic 발견의 실마리 제공.)

### 무엇이 진짜 참인가 (robust하게)
- ✅ 직진 주행 균형 (v≥0.5), ✅ ~5° heading 미세보정 → **robust**.
- ⚠️ ≥10° 선회 → **chaotic/marginal** (되기도 하고 안 되기도, 예측 불가). robust하게
  "된다"고 주장 불가.
- 40° 영상(mm_turn40_falls.mp4)은 **한 chaotic 실현**(이 IC에서 25s 낙하)일 뿐,
  결정론적 거동 아님.
- kd_yaw는 여전히 역효과(roll 섞임) → 0 유지.

### 함의 (연구 — 오히려 강력)
이건 **LQR이 이 플랜트에서 부적합함을 보이는 결정적 증거**다: 약한 액추에이터 →
게인 포화 → chaos. 제약을 명시 처리하는 **MPC** 나 **RL** 이 정확히 필요해지는 지점.
제안서의 "RL vs 고전 robustness 비교"에서 고전(LQR)의 실패 모드를 정량화한 핵심 데이터.
authority를 키우면(무게추↑/스트로크↑) 포화가 풀려 선회가 robust해지는지는 다음 스윕 대상.

### ⚠️ 수치 marginality (중요한 함정)
이 컨트롤러는 내부루프가 F포화 ~87%인 **거의 bang-bang**이라 수치적으로 예민:
**동일 config가 CPU-JAX vs GPU-JAX에서 다른 궤적**(turn40 slew10: GPU 5° dip / CPU 25° dip;
S커브는 둘 다 전복). 배포 경로=CPU(numpy 추론)이므로 **CPU 기준으로만 성능 주장**할 것.
엔벨로프 표도 전부 CPU 측정. 교훈: 포화 지배 마진 컨트롤러는 플랫폼 민감 → 학습(MJX=GPU)
정책을 실기(CPU)로 옮길 때 sim2sim 갭 주의.

### CPU 단일 선회 엔벨로프 (최대 lean°, 생존 시, v=2)
| 목표°\slew | 3°/s | 5°/s | 10°/s | 20°/s |
|---|---|---|---|---|
| 10° | 3.4 | 전복 | 3.4 | 3.4 |
| 20° | 4.0 | 전복 | 19.9 | 25.5 |
| 30~60° | 3.8 | 3.6 | 22.9 | 전복 |

slew 5°/s의 비단조성(10·20°는 전복, 30°+는 생존)이 marginal/chaotic의 증거.
방향반전 ±target(@2s,10s, slew10): ±10 전복 / ±15 생존 / ±20·±30 전복 — 역시 비단조.

### 구조 (`mm_controller.py`)
```
yaw_ref → heading(yaw오차→lean_ref, ±lean_max) → balance_mass(lean을 lean_ref로) → 힘
```
- `balance_mass(st,g,lean_ref)`: `-(K·(x−x_ref))`, x_ref=[lean_ref,0,0,0].
- `heading`: `lean_ref=-(k_yaw·yaw_err−kd_yaw·yaw_rate)`, `clip(±lean_max)`. lean_max=0.5°.
  좌회전(yaw+)엔 왼쪽 기울기(lean−) → 앞의 음수.
- `v_fwd`=**body-forward**(v[0]cosψ+v[1]sinψ). world-x로 추종하면 선회 시 속도루프 헛돌아
  감속·전복 (실제 발생 → 수정).
- yaw_ref는 호출부에서 **슬루 3°/s** (완만 영역).

### 구조 (`mm_controller.py`)
```
yaw_ref → heading(yaw오차→lean_ref, ±lean_max 제한) → balance_mass(lean을 lean_ref로) → 힘
```
- `balance_mass(st,g,lean_ref)`: `-(K·(x−x_ref))`, x_ref=[lean_ref,0,0,0].
- `heading`: `lean_ref = -(k_yaw·yaw_err − kd_yaw·yaw_rate)`, `clip(±lean_max)`.
  부호: 좌회전(yaw+)엔 왼쪽 기울기(lean−) 필요 → 앞의 음수. lean_max=0.5°(0.0087rad).
- `v_fwd` = **body-forward** 속도(v[0]cosψ+v[1]sinψ). world-x로 추종하면 선회 시
  속도루프가 헛돌아 감속·전복 (실제로 발생 → 수정).
- yaw_ref 는 호출부에서 **슬루 제한 10°/s** (급격한 기준 반전이 전복 유발 — 아래).

### 발견/함정
1. **직진 K가 리밋사이클이면 조향에서 터진다.** 정지-검증용 공격적 K(4°,20N)는 즉시
   포화 bang-bang → 직진에서도 lean ±2° 리밋사이클. '생존'은 하나 yaw_ref 램프와 위상이
   맞으면 전복. → 운전점 재설계 **(10°,5N)**: v=2.0 직진에서 lean RMS 0.0°(진짜 수렴).
   교훈: 조향은 내부루프의 *정상상태 품질*을 드러낸다(생존≠수렴).
2. **급기준(슬루) 한계**: yaw_ref 슬루 25°/s까지 OK, 12°/s↓에서 오히려 전복 —
   느린 기준이 lean 을 한 방향으로 오래 물려 스트로크 소진. 10°/s 채택.
3. **선회는 본질적으로 완만**: lean_max=0.5°면 v=1 지속선회 R≈16.5m. 무게추 조향의
   근본 한계(지속 lean 유지 = 스트로크 상시 점유). 급선회엔 부족 — RL/큰 스트로크 여지.

### 검증 (v=2.0, 0.5° 초기섭동, slew 10°/s)
- +30° 스텝: 20s, 최종 yaw 28.6°, max lean 4.4°, F포화 87%(과도구간)
- S커브(+30/−30/0): 렌더 확인 (mm_scurve.mp4)
- 지속선회 R≈16.5m(v=1, lean_max 0.5°)

### 캐비앳
운전점 v=2.0·K(10°,5N)·lean_max 0.5°·slew 10°/s 한정. 정지~저속 조향은 미검증
(정지 균형 자체가 LQR 불가). heading gain(k_yaw=0.2)은 손튜닝 — MIMO/RL 로 흡수 여지.

### 파일 갱신
`mm_controller.py`(heading 추가), `mm_lqr.py`(운전점 (10°,5N)+heading gains 저장),
`mm_render.py`(MM_YAW 모드+슬루). 영상 `mm_turn30.mp4` `mm_scurve.mp4`(커밋 제외).

## 2026-07-07 — 플랜트 구축 + 4-state LQR: 저속 주행 균형 성공, 정지는 제약 지배로 실패

### 결론 (제안서 1단계 직결)
- **v ≥ 0.5~0.75 m/s에서 moving-mass 단독(free fork) 균형 20s 성공** — 무게추→lean→
  self-steering 동원이라는 제안서 핵심 메커니즘이 시뮬에서 작동함을 확인.
- **정지(v=0)는 전 파라미터 조합에서 LQR 실패** — 힘/스트로크 제약이 지배하는 구조적 한계.
- **설계 반직관 발견: 추는 가벼울수록 좋다** (힘 한계 고정 시). 하드웨어 설계에 직접 시사.

### 플랜트 (`moving_mass_bicycle.xml`, sanity 5/5 통과)
reaction-wheel 플랜트에서 파생: trail 74mm 포크·얇은 타이어·접촉제외 유지.
플라이휠/조향모터 제거(free fork), 안장 위(h=0.55m) 횡방향 슬라이더(기본 2kg, ±0.15m,
힘입력 ±20N, 비충돌 geom). M=13.2kg, h_CoM=0.519m, I0=4.213kg·m², 불안정 τ=0.250s.
`mm_model.build(m,d)`로 파라미터 변형. sanity: 인덱스/내부접촉(유령브레이크 검사)/trail/
역진자(2°→1.07s)/free-fork self-steering(기운 쪽 ✓).

### 4-state LQR (`mm_lqr.py`)
접촉선 기준 라그랑주: `[I0, −m·hm; −m·hm, m][θ̈;ÿ] = [Mghθ − mgy; F − mgθ − cẏ]`.
**부호 함정 3곳**(y+=왼쪽, lean+=오른쪽 규약): 오프셋 토크 −mgy, 결합항 −m·hm, 레일중력
−mgθ. 처음에 +로 썼다가 LQR이 추를 넘어지는 쪽으로 미는 증상(passive보다 빨리 낙하,
슬라이더 스톱 고정)으로 발각·수정. 시스템 ID로 모델 검증: 임펄스 응답(ẏ, roll̇) 오차
4~9%, 비최소위상 방향 일치.

### 왜 정지(v=0)는 안 되나 — 제약 지배 (모델 오류 아님)
- 0.1° 섭동에서도 선형 폐루프가 요구하는 스트로크 ≈ 0.21m > 0.15m 물리 한계.
- 불안정극 과도증폭(τ=0.25s) vs 추 이동시간의 경쟁 + 비최소위상 반작용 + 스톱 충돌.
- gain을 어떻게 잡아도 (강함→힘 포화 bang-bang / 약함→스트로크 초과) 실패.
  Bryson 4개 변형 전부 동일 낙하시간(1.0~2.7s)이 그 증거 (포화 지배 = gain 무관).
- 오답 노트: "무게추 고정 서보" 정역학 테스트는 검증이 아님 — 상수 오프셋은 넘어지는
  방향만 바꿈 (T2/T3에서 lean이 0을 지나 반대쪽으로 감 = 토크 권한 자체는 공식대로).
  fork-flop 가설도 기각 (포크 잠가도 동일).

### 설계영역 스윕 (0.5° 섭동, 20s 기준, per-config LQR 재설계, F=20N 고정)
| m\d | 0.10m | 0.15m | 0.20m |
|---|---|---|---|
| 1kg | v_min=0.75 | 0.75 | 0.75 |
| 2kg | 0.75 | **0.50** | — |
| 3kg | 0.75 | — | — |
| 4kg | — | — | — |

- **v=0 가능 조합 없음.** 최적: 2kg×0.15m (v_min=0.50).
- **무거울수록 나빠짐**: τ 내 가용 토크 `½F·g·τ²`는 질량 무관인데 반작용 관성(m·hm)은
  질량 비례 → 힘 한계 고정이면 가벼운 추가 우월. 스트로크 증가도 역효과(과도 시간↑).
- 하드웨어 시사: 슬라이더 구동력(벨트 장력/모터 토크) 예산이 추 질량보다 중요.

### 한계/캐비앳 (인용 시 주의)
F=20N·Bryson(4°,20N)·마운트 높이 0.55m 고정, 섭동 0.5°만, LQR 한정(MPC/RL은 v=0
경계를 밀어낼 수 있음 — 제약 인지 제어가 정확히 유리한 지점이라 RL 비교 명분 강화).

### 다음
- RL(PPO/SAC) 학습 → 동일 지표(v_min, 회복한계) LQR 대비 — 제안서 본론.
- F_max 축 추가한 3D authority 스윕, 마운트 높이 축.
- 정지 근처: MPC(제약 명시) 또는 fork lock/damper 하이브리드 검토.

### 파일
`moving_mass_bicycle.xml` `mm_model.py` `mm_sanity.py` `mm_lqr.py` `mm_controller.py`
`mm_lqr_gains.json` `mm_render.py` / 영상 `mm_ride_1ms.mp4`(커밋 제외)
